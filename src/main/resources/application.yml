spring:
    datasource:
        url: jdbc:mysql://localhost:3306/library
        username: root
        password:
        driver-class-name: com.mysql.cj.jdbc.Driver
    jpa:
        hibernate:
            ddl-auto: 'update'
logging:
    level:
        web: DEBUG